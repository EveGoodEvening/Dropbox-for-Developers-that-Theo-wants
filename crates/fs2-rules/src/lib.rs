//! `fs2-rules`: `.fs2ignore` and `.fs2/config.toml` rule engine.
//!
//! Placeholder module; implementation lands in a later phase.

#![forbid(unsafe_code)]

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        // Keeps this crate in `cargo test --workspace` with a passing result
        // until the real rule engine lands.
    }
}
